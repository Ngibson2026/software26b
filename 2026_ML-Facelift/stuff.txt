bobby
B0bby15Stup!d

update base.html to include the new extra_head block
had to remove the commented code from quote quote_stock.html
pip install plotly
add graph to quote stock, and the extra head block, plus the footer script 

quotestock
<!--
    <form method="POST" action="{{ url_for('quote_stock') }}">
        {{ form.csrf_token }}

        WTForms
        <p>Stock Ticker {{ form.tickerName() }}</p>
        <p>{{ form.submit }}</p>

    </form>
    
    {% if stock_data %}
        {{ stock_data.ticker }} - {{ stock_data.name }}
        ${{ stock_data.current_price }} {{ stock_data.currency }}
        <strong>Previous Close:</strong> ${{ stock_data.previous_close }}
        <strong>Last Updated:</strong> {{ stock_data.last_updated }}

        {% if stock_data.summary %}
            <hr>
            <p><strong>About the company:</strong><br>
            {{ stock_data.summary[:650] }}...</p>
        {% endif %}
    {% endif %}
 -->

 <form method="POST" action="{{ url_for('buy_stock') }}">
    <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
    <input type="hidden" name="ticker" value="{{ stock_data.ticker }}">
    <label class="form-label">Number of Shares</label>
    <input type="number"
        name="shares"
        class="form-control"
        min="1"
        value="10"
        required>
    <label class="form-label">Price per Share</label>
    <input type="text"
        class="form-control"
        value="${{ stock_data.current_price }}"
        readonly>
    <button type="submit" class="btn btn-primary btn-lg w-100">
        Buy Shares
    </button>
</form>